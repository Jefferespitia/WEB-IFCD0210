// Crea una función que saque por consola 
// el área de un rectángulo 
// de dimensiones dadas como parámetro. (base, altura)


function rectangleArea(b, h) {
    const result = b * h
    return result
}

const b = 3
const h = 2
console.log(rectangleArea(b, h))
